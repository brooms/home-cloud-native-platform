---

- hosts: consul_instances

  become: true
  
  vars:
    consul_dnsmasq_enable: true

  tasks:

    - import_role:
        name: brianshumate.consul
      tags:
        - consul

    - debug:
        var: consul_servers
